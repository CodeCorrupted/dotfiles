[module/date]
type = internal/date
interval = 1

date = %H:%M
date-alt = %a-%d-%m-%Y

format = <label>
format-underline = ${colors.date}
format-prefix = %{T2} %{T-}
format-prefix-foreground = ${colors.date}
format-background = ${colors.bg-modules}
format-prefix-background =  ${colors.bg-modules}

label = %date%
label-foreground = ${colors.fg}
